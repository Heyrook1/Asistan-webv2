# P0.8 — Paid-pilot security gates (go / no-go)

**Rule:** Do not open paid production until every row below is **CLOSED** or accepted residual with owner sign-off.

**Offline evidence:** `pnpm verify:p0.8`  
**Live evidence:** `pnpm smoke:asistan-app-rls` + `pnpm smoke:cross-tenant` + `pnpm check:production`

Last review: **2026-08-10**

| # | Risk | Status | Evidence | Verify |
|---|------|--------|----------|--------|
| 1 | Cross-tenant access | **PARTIAL → ops CLOSED** | Tenant guard + GUC RLS; `smoke:cross-tenant` (9 IDOR); CI `tenant-isolation` | `pnpm smoke:asistan-app-rls` + `pnpm smoke:cross-tenant` on prod-like DB with `asistan_app` |
| 2 | Owner → SUPER_ADMIN | **CLOSED** | `platformRoleAssignmentError` in `lib/actions/team.ts` | `pnpm test -- team-super-admin-gate` |
| 3 | Auth fail-open | **CLOSED** | `proxy.ts` prod fail-closed; cron/WA fail-closed | `pnpm test -- cron-auth`; e2e `critical-gates` |
| 4 | Person/identity wrong match | **CLOSED** | `shouldAutoLinkPerson` ≥2 strong signals (hash-alone blocked) | `pnpm test -- identity-normalize` |
| 5 | Booking idempotency | **CLOSED** | In-tx claim + unique `keyHash` | `pnpm test -- booking-idempotency` |
| 6 | Double-booking | **CLOSED** | Day-row `FOR UPDATE` + `pg_advisory_xact_lock` on slot key | Code review `create-slot-appointment.ts`; concurrent book staging |
| 7 | SMS/WhatsApp soft no-op | **CLOSED** (visibility) | `not_configured` / `error` statuses; board + settings UI | `pnpm test -- patient-channel-delivery`; unset providers → visible label |
| 8 | Production rate limiting | **PARTIAL** | Upstash preferred; memory OK single-node; Upstash errors fail-closed | `pnpm test -- rate-limit-memory-fallback`; set Upstash before multi-instance |
| 9 | Prisma / migration gaps | **CLOSED** (tooling) | Supabase SQL authority + drift/RLS checks | `pnpm check:schema-drift` + `pnpm check:production` |

## Paid-pilot blockers (must be green)

1. Runtime `DATABASE_URL` uses **`asistan_app`** (NOBYPASSRLS) — not owner.
2. Both tenant smokes PASS against that DB.
3. `pnpm verify:p0.8` PASS.
4. If more than one app instance: **Upstash Redis** configured.
5. SMS/WA: either configured for delivery or operators accept fail-visible `yapılandırılmadı` (booking still succeeds by design).

## Residual (sign-off, not silent)

| Item | Residual | Acceptable for pilot? |
|------|----------|------------------------|
| Cross-tenant | App-layer still required if someone points `DATABASE_URL` at owner | Only if ops checklist proves `asistan_app` |
| Rate limit | Memory under-enforces multi-node | Only single-node EC2 **or** Upstash set |
| Channel delivery | ≥80% provider SLA is ops, not CI | Yes if fail-visible |

## Related

- [`docs/security-ops.md`](./security-ops.md)
- [`docs/trust-control-matrix.md`](./trust-control-matrix.md)
- [`docs/ci-perf-tenant-gate.md`](./ci-perf-tenant-gate.md)
- [`docs/gelistirme-raporu-2026-07-20.md`](./gelistirme-raporu-2026-07-20.md)
